# Core — geometric backbone (Stage A), KG, place layer, figures. CPU-only.
numpy>=2.0
open3d>=0.19
pandas>=2.0
pyyaml
pye57>=0.4          # .e57 input (multi-setup files supported)
scipy>=1.14
matplotlib>=3.9
scikit-image>=0.22   # SLIC place segmentation

# Stage B — multi-view VLM verification (needs ANTHROPIC_API_KEY).
anthropic>=0.40

# Optional learned baselines (Uni3D / PointCLIP V2 / SPFormer / PTv3 /
# Point-SAM). Not needed for the proposed pipeline. Install torch matching
# your CUDA driver, then follow patches/README.md for the pinned upstream
# repos and weights.
# torch
